# Pink Lady Landing — corrected

This deliberately preserves the existing V2 hero/cover artwork unchanged.

The established circular Pink Lady profile icon is added only as the small
brand/logo asset used in the header (`pink-lady-logo.png`). It does NOT
replace the hero image.

Daily new arrivals are controlled only by `latest.json`; the permanent
website does not need redesigning for each collection.
